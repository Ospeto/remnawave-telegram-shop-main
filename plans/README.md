# Implementation Plans

Generated by the improve skill on 2026-06-20. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

Planning base commit: `8e80b0b`.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| [001](001-referral-service-purchase-only.md) | Restrict referral conversion bonuses to service purchases | P1 | M | - | DONE |
| [002](002-api-me-minimal-user-dto.md) | Return a minimal user DTO from `/api/me` | P1 | S | - | DONE |
| [003](003-validate-screenshot-mime.md) | Validate screenshot bytes before vision analysis | P1 | S-M | - | DONE |
| [004](004-browser-security-headers.md) | Add browser security headers for the Mini App | P2 | M | 002, 003 | DONE |
| [005](005-remove-dead-wallet-package.md) | Remove the unused duplicate wallet service package | P2 | S-M | 001 | DONE |
| [006](006-document-legacy-wallet-autorenew.md) | Correct docs for the removed wallet auto-renew endpoint | P3 | S | - | DONE |
| [007](007-frontend-dependency-drift.md) | Refresh frontend dependencies and clear audit/tooling drift | P3 | S-M | - | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency Notes

- Plan 004 should land after Plans 002 and 003 so browser hardening tests can validate the settled API and upload behavior without mixing unrelated contract changes.
- Plan 005 should land after Plan 001 because both touch wallet/payment concepts; keep the money-flow behavior change separate from dead-code cleanup.
- Plans 006 and 007 are independent and can run anytime, but they are lower priority than the runtime behavior fixes.

## Verification Baseline

Use these commands as the common final gate unless a plan narrows the scope:

| Purpose | Command | Expected on success |
|---------|---------|---------------------|
| Go tests | `go test ./...` | exit 0 |
| Go vet | `go vet ./...` | exit 0 |
| Go build | `go build ./cmd/app` | exit 0 |
| Frontend tests | `cd web-app && npm test` | exit 0 |
| Frontend build | `cd web-app && npm run build` | exit 0 |

The CI workflow at `.github/workflows/ci.yml` runs the same gates, with Node 20 and `npm ci` for the frontend.

## Findings Considered And Rejected

- Client-only auth bypass: rejected. Mini App auth validates signed Telegram `initData` server-side and exchanges it for server-issued sessions.
- Redirect page direct open redirect/XSS: rejected as an active exploit finding. Redirect tokens are consumed server-side and the redirect page JSON-encodes target values. Browser header hardening remains planned in Plan 004.
- Backup restore exposed at runtime: rejected. The bot handler reports runtime restore as disabled and points operators at offline/manual restore.
- CryptoPay invoice checker as urgent runtime risk: rejected. CryptoPay is intentionally disabled in the current runtime; stale disabled-rail cleanup can be revisited later if CryptoPay is reactivated.
